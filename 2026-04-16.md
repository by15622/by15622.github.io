2026-04-16 학습 기록

## 오늘 한 것
- 리뷰 목록 조회 API 구현(커서 기반 무한 스크롤): QueryDSL을 활용하여 검색 조건과 커서(Cursor)를 기반으로 데이터를 끊어오는 레포지토리 로직을 서비스 및 컨트롤러 계층까지 완벽하게 도움받아 연동 완료하였습니다.
- 팀 컨벤션에 맞게 동기화 및 리팩토링을 진행하였습니다. dto 네이밍이 기존에 ~Response 명칭을 팀 규칙에 맞게 ~Dto로 통일하였고, 프론트엔드와 통신하기 위해 팀에서 약속한 커스텀 헤더(Deokhugam-Request-User-ID)로 어노테이션 전면 수정하였습니다. 마지막으로 팀 공통 페이지네이션 응답 객체인 CursorPageResponseDto 구조를 분석하고, 내 도메인의 데이터를 해당 규격에 맞춰 성공적으로 반환하도록 설계 변경하였습니다.
## 오늘 이슈
- 요구사항 변경으로 인해 좋아요 여부를 판단할 '로그인 유저 ID'가 추가로 필요해졌고, 공용 DTO 박스는 6개의 데이터를 요구하는데 기존 코드에서는 이를 다 채우지 못해 발생한 문제가 일어났고, 컨트롤러에서 @RequestHeader로 유저 ID를 받아 서비스로 안전하게 넘겨주도록 수정. 서비스 계층에서는 팀의 CursorPageResponseDto가 요구하는 6가지 규격(content, nextCursor, nextAfter, size, totalElements, hasNext)에 맞춰 데이터를 넣어 해결하였습니다.
## 한 줄 요약
 오늘로 3일차 팀 프로젝트 너무 어렵다..