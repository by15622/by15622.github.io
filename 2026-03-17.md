2026-03-17 학습 기록

## 오늘 한 것
- 백엔드 API와 프론트엔드 간의 데이터 규격을 맞추고 렌더링 에러를 해결했습니다. 그리고 스위치 토글 시 데이터베이스에 상태가 저장되지 않던 문제를 해결했습니다.
- 공공데이터포털 OpenAPI 연동 최적화: 금융위원회 시장지수정보 API를 호출하여 실시간 데이터를 가져오는 로직을 도움받아 개선했습니다.
## 오늘 이슈
- 조회된 데이터가 없을 때 특정 필드가 null로 내려가 프론트엔드 함수에서 에러 발생하였는데, 백엔드 DTO반환시 null 대신 0L등 기본값을 보장하도록 방어로직을 추가하였다.
- 서비스 메서드에 @Transactional이 누락되어 DB반영 Commit이 안되었는데, @Transactional 어노테이션 추가로 데이터 정성성을 확보하였습니다.
## 한 줄 요약
백엔드는 항상 데이터가 없을 때 null을 대비해 안전한 기본값을 내려주는 습관을 들여야 한다는걸 느꼈다.