2026-04-30 학습 기록

## 오늘 한 것
- 오늘은 프로젝트 최종 발표를 앞두고 주요 PR을 머지(Merge)하며 실서버 배포 준비를 마쳤습니다. 코드를 새로 작성하기보다는, 그동안 리뷰(Review) 도메인을 개발하면서 마주쳤던 심각한 성능 저하(N+1) 문제와 데이터베이스 무결성 이슈를 되돌아보고, 이를 어떻게 아키텍처 관점에서 해결했는지 트러블슈팅 과정을 정리했습니다.
## 오늘 이슈
- 리뷰 목록 10개를 조회하는 API를 호출할 때, 총 31번의 SELECT 쿼리가 데이터베이스에 쏟아지는 엄청난 성능 병목을 발견했습니다. 원인으로 기존에는 DB에서 Review 엔티티만 먼저 가져온 뒤, Service 계층에서 DTO로 변환하는 반복문을 돌렸고 이 과정에서 각 리뷰의 '도서 정보(Book)', '작성자 정보(User)', '현재 사용자의 좋아요 여부(ReviewLike)'를 가져오기 위해 매번 추가 단건 쿼리가 발생하고 있었습니다 (1 메인 쿼리 + 10 + 10 + 10). 해결방안으로 Service 계층의 변환 로직을 걷어내고, QueryDSL의 Projections.constructor를 전격 도입해서 Book과 User 테이블을 INNER JOIN 하여 DB 단에서 한 번에 조립한 뒤 DTO에 직접 꽂아주도록(Direct Mapping) 변경하였습니다. 그리고 '좋아요 여부(likedByMe)'는 JPAExpressions를 활용한 EXISTS 서브쿼리로 통합했습니다. 
## 한 줄 요약
오늘은 기능을 전반적으로 점검하면서 단순히 기능만 돌아가는게 아니라 혹시나 다른 이슈가 없을까 찾아본 하루였던거 같다.