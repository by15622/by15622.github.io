2026-02-05 학습 기록

## 오늘 한 것
- 포스트맨 환경(Environment) 및 폴더 관리를 해보고 base_url과 user_id를 환경 변수로 설정하여 중복 주소 입력을 방지했습니다. 그리고 Collections를 생성하여 실습별로 요청(Request)을 분류하고 저장하는 법을 익혔습니다.
- 스프링 프로젝트 구조 최적화 컨트롤러들을 controller 패키지에 모으고, 데이터 전송 객체인 UserRequest를 관리하기 위해 dto 패키지를 새로 만들어 구조를 정리했습니다.
## 오늘 이슈
- 서버 기동 오류 (Bean 중복 매핑)동일한 주소(/hello)를 가진 컨트롤러가 두 개 있어 서버가 켜지지 않았습니다. 이거에 대해서 주소를 변경하거나 중복된 컨트롤러를 정리하여 해결했습니다.
## 한 줄 요약
포스트맨은 무엇인가 이거에 대한 궁금증이 생겼습니다