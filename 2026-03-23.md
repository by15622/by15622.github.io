2026-03-23 학습 기록

## 오늘 한 것
- 새로운 모듈을 만들고 application.yml 파일을 새롭게 작성하여 PostgreSQL 데이터베이스 연결 정보(url, username, password)를 설정하고, 서버가 정상적으로 DB를 인식하도록 만들었습니다.
- 중복된 이메일(test@test.com)로 가입 요청을 보냈을 때 서버에서 DuplicateEmailException이 터지도록 실습했습니다. 그리고 예외 처리를 따로 해주지 않으면 클라이언트(포스트맨)에게 500 Internal Server Error가 반환된다는 것을 직접 눈으로 확인했습니다.
## 오늘 이슈
- 폴더명은 SpringStableHigh인데, 자꾸 예전 이름인 SpringException 경로를 찾으며 빌드가 실패했습니다. settings.gradle에서 include된 모듈 이름이 제대로 되어있는지 확인하고, build.gradle을 우클릭해 새로고침을 진행하여 인텔리제이의 꼬인 캐시를 풀어주어 해결했습니다.
## 한 줄 요약
로그를 보는 실습을 하는데 조금 어렵다