2026-04-23 학습 기록

## 오늘 한 것
- '리뷰' 도메인과 팀원분이 만드신 '댓글' 도메인의 연동 테스트를 진행하였습니다. 요구사항에 맞춰 하나의 리뷰에 여러 개의 댓글이 달리는 것을 확인했고, 가장 중요한 댓글 등록/수정/삭제 시 리뷰의 commentCount가 실시간으로 동기화되는 것을 확인했습니다.
- 프론트엔드 파트의 피그마(Figma) UI 시안을 보면서, Review API들이 화면의 어느 요소에 들어가는지 체크했습니다. 무한 스크롤 정렬 버튼 -> ReviewListRequest의 동적 정렬 파라미터, 비어있는 인기 리뷰 화면 -> Map.of("content", ...)로 감싸서 보낸 에러 방지용 응답 구조 화면에 필요한 모든 데이터 파이프라인이 백엔드에 빈틈없이 준비되어 있음을 확인하고 다시 한번 더 체크하였습니다.
## 오늘 이슈
- 어제 논리 삭제(Soft Delete) 로직을 고도화하면서, DB 레벨의 @UniqueConstraint를 삭제하고 애플리케이션 단의 검증 로직으로 변경하였는데, 배포 서버에서 테스트를 해보니 똑같이 violates unique constraint "uq_reviews_book_user" 에러가 나타났습니다. 원인으로 JPA ddl-auto=update 옵션이 문제가 있었고, 이 옵션은 엔티티에 새로운 컬럼이 생기면 테이블에 추가는 잘 해주지만 자바 코드에서 제약 조건을 지웠다고 해서 실제 db에 걸려있는 제약조건까지 drop 해주지 않는다고 배웠습니다. 결국 배포 서버 db에는 예전에 걸어둔 인덱스가 그대로 살아 있어서 배포 담당자분께 ALTER TABLE reviews DROP INDEX uq_reviews_book_user; 요청을 드려서 쿼리를 날려 예전 제약조건을 수동으로 삭제하였습니다.     
## 한 줄 요약
역시 눈으로 딱 보이는 배포 프론트쪽이 보기편하다 