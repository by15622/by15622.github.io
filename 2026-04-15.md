2026-04-15 학습 기록

## 오늘 한 것
- 리뷰 서비스 핵심 로직을 구현하였습니다. 도서에 대한 리뷰 등록(Create), 수정(Update), 삭제(Delete) 기능을 구현하고, 데이터를 DB에서 완전히 날리는 물리 삭제 대신, deleted_at 컬럼에 시간을 기록하는 논리 삭제 방식으로 구현하여 데이터 추적성을 높였다. 마지막으로 리뷰가 등록될 때마다 수십만 개의 데이터를 다시 더하는 대신, (기존 총점 + 새로운 평점) / 새로운 리뷰 개수 공식을 적용해 O(1) 성능으로 도서 평균 평점이 즉각 업데이트되도록 로직을 구성했다.
- 기존의 IllegalArgumentException들을 설정해둔걸, 팀원 분이 세팅해 주신 CustomException과 ErrorCode로 전면 교체했다. 중복 리뷰 작성(DUPLICATE_REVIEW), 남의 리뷰 수정 시도(UNAUTHORIZED_ACCESS) 등 상황에 맞는 정확한 HTTP 상태 코드와 메시지를 프론트엔드에 전달해 줄 수 있게 되었다.
## 오늘 이슈
- 컨트롤러 테스트를 위해 서버를 켜려고 하니, 알림(Notification) 엔티티 쪽에서 컴파일 에러(cannot find symbol class BaseUpdateableEntityBuilder)와 빨간 줄이 발생했다. 해결방안으로 자식 클래스에서 @SuperBuilder를 쓰려면 부모 클래스에도 있어야 한다. 공통 폴더의 BaseUpdateableEntity 파일 맨 위에 @SuperBuilder를 추가해 해결했다. 그리고, User라는 단어가 DB 예약어와 겹쳐 우리 팀은 테이블/엔티티명을 Users로 쓰기로 했었다. 알림 엔티티에 잘못 적혀있던 User 타입을 모두 Users로 수정하여 빨간 줄을 없앴다.
## 한 줄 요약
분명히 어제는 괜찮았는데 이제부터 너무 어렵다..