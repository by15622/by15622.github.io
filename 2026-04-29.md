2026-04-29 학습 기록

## 오늘 한 것
- 프로젝트의 로컬 DB 세팅부터 시작해서, 리뷰기능의 논리 삭제 테스트와 대시보드 연동까지 백엔드 전반의 흐름을 점검하였습니다. 그리고 데이터베이스의 물리적 제약과 애플리케이션의 논리적 처리 사이의 충돌을 겪고 도움받아 해결하였습니다. 
## 오늘 이슈
- DataGrip을 통해 로컬 DB를 세팅하는 과정에서 public 스키마 접근 권한 없음 에러 발생. 비밀번호 오류 로그가 외계어(ġ:)처럼 깨지는 현상도 겪었습니다. 원인으로 인코딩 깨짐은 DB(EUC-KR)와 접속 툴(UTF-8)의 인코딩 불일치 때문이였고, 스키마 권한 에러는 PostgreSQL 15 버전부터 강화된 보안 정책 탓에, 일반 유저 계정이 public 스키마에 테이블을 생성할 수 없었습니다. 해결방안으로 최고 관리자(postgres) 계정으로 접속하여 유저에게 스키마 사용 권한(GRANT ALL ON SCHEMA public TO 유저명;)을 부여하여 해결하였습니다. 
- 도서 리뷰를 삭제한 뒤 다시 똑같은 도서에 리뷰를 재등록하려고 하니 409에러와 함께 DataIntegrityViolationException (유니크 제약 위반) 발생하였습니다. 원인으로 자바 코드에서는 논리 삭제(deleted_at IS NOT NULL)를 적용해 데이터가 지워졌다고 판단했지만, DB 테이블에는 UNIQUE (book_id, user_id)라는 강력한 물리적 제약조건이 걸려있어 이미 존재하는 데이터로 인식하고 튕겨내었고 기존의 무조건적인 UNIQUE 제약 조건을 DROP 하고, 부분 유니크 인덱스 (Partial Unique Index)를 도입하여 해결하였습니다.
## 한 줄 요약
오늘 정말 많은 에러를 만난날인데 DB의 제약 조건과 스프링부트가 어떻게 상호작용하는지 깊게 파고든 하루였다.