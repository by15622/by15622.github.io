2026-03-05 학습 기록

## 오늘 한 것
- 학원에서 제공해준 dist 폴더 내의 파일들을 스프링 부트의 src/main/resources/static 경로에 배치하여 실제 웹 화면을 구현했습니다. 직접 만든 API를 통해 화면에서 회원가입과 로그인이 정상적으로 작동하는지 확인했고, 작성한 MessageController와 ChannelController가 프론트엔드 UI와 통신하여 메시지를 주고받는 흐름을 파악했습니다.
## 오늘 이슈
- (400 Bad Request) 회원가입을 하려니까 이미 있다고 하고, 로그인하려니 없다고 하는 데이터 꼬임 현상이 발생했습니다. PostgreSQL 툴에서 TRUNCATE 명령어로 테이블 데이터를 초기화하고, application.yaml의 ddl-auto 설정을 다시 점검하여 해결했습니다.
- 브라우저 보안 정책으로 인해 프론트엔드에서 백엔드 API로의 접근이 차단되었습니다. 그래서 모든 컨트롤러 클래스 상단에 @CrossOrigin(origins = "*") 어노테이션을 추가하여 외부 요청을 허용했습니다.
## 한 줄 요약
실습 난이도가 너무 높아서 어렵습니다