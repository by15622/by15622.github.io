2026-04-08 학습 기록

## 오늘 한 것
- 실습8 미션의 핵심 과제인 GitHub Actions를 활용한 CI/CD 파이프라인 구축을 진행함. 로컬 환경에서의 수동 테스트 및 배포를 벗어나, 코드 푸시부터 AWS 인프라 배포까지의 전 과정을 코드로 작성하여 자동화 환경을 구성하였다.
- GitHub Actions 워크플로우(test.yml)를 작성하여, 레포지토리에 코드가 푸시될 때마다 독립된 환경에서 자동으로 빌드 및 테스트가 수행되도록 설정함. 테스트 코드의 신뢰성을 확인하고 정량적인 지표를 확보하기 위해 프로젝트에 JaCoCo 플러그인을 적용하여 테스트 커버리지를 측정하였고, 측정된 커버리지 리포트를 Codecov 서비스로 자동 전송하도록 파이프라인을 연동함. 발급받은 Markdown 뱃지 코드를 프로젝트 README.md 최상단에 배치하여, 깃허브 레포지토리 메인 화면에서 실시간 커버리지 퍼센티지(현재 83%)를 직관적으로 확인할 수 있도록 시각화 처리를 완료하였다.
## 오늘 이슈
- 로컬 개발 환경의 개인 설정 폴더(.idea, .discodeit)를 제외하기 위해 .gitignore에 명시했으나, 원격 리포지토리(GitHub)에 해당 폴더들이 계속해서 업로드되는 이슈 발생. 원인으로 기존에 작성한 .gitignore 파일은 프로젝트 하위 디렉토리(discodeit/) 내부에 위치했던 반면, 타겟 폴더들은 프로젝트 최상위(Root) 경로에 존재하여 제외 규칙이 적용되지 않음. 해결방안으로 CLI를 통해 프로젝트 최상위 루트 디렉토리로 이동한 후, 루트 레벨의 전역 .gitignore 파일을 생성하고 예외 처리할 폴더명(.idea/, .discodeit/)을 추가해서 git rm -r --cached <폴더명> Git의 추적 캐시만 명시적으로 삭제하였다
## 한 줄 요약
오늘 배포에서 잡아먹은 시간이 너무커서 정신이 없는 하루였다.