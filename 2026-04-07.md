2026-04-07 학습 기록

## 오늘 한 것
- AWS ECR 리포지토리를 프라이빗의 용량 제한을 피해 Public 레포지토리(discodeit)를 생성하였습니다. 그리고, IAM 권한 설정했는데 ECR에 접근하기 위해 사용자에게 AmazonElasticContainerRegistryPublicFullAccess 권한을 부여하였습니다. CLI 및 Docker 환경 세팅 관련해서도 aws configure를 통해 프로필을 교체하고, 도커 자격 증명 오류를 해결하기 위해 config.json 설정을 도움받아 수정하였습니다. 마지막으로, docker buildx를 사용하여 amd64와 arm64 아키텍처를 동시에 지원하는 이미지를 빌드하고 ECR에 최종 업로드 성공하였습니다.
## 오늘 이슈
- ECR 로그인 시 권한 거부 에러 발생하였고, 원인이 터미널이 기존의 dev-codeit 유저로 설정되어 있어, 새 유저(discodeit)의 권한을 사용하지 못하였습니다. 해결로는 aws configure로 유저를 교체하고 IAM에서 정확한 ECR 전용 권한을 추가하였습니다.
- docker login 시 윈도우 시스템과 충돌하며 로그인 실패하였고, 원인이 Docker Desktop의 credsStore 설정이 윈도우 자격 증명 관리자와 꼬임현상이 있었는데 .docker/config.json 파일에서 "credsStore": "desktop" 줄을 삭제하여 설정을 초기화하였습니다.
## 한 줄 요약
어렵다.. aws는 설정할게 너무 많은 중요한 친구다