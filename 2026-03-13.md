2026-03-13 학습 기록

## 오늘 한 것
- (No-offset 페이징을 도입)수만 개의 데이터를 한 번에 가져오면 서버에 부하가 가기 때문에, lastId를 활용한 No-offset 방식(Slice)을 적용해 조회 성능을 최적화했습니다. 그리고 제네릭(<T>)을 활용하여 API 응답 구조를 표준화했습니다. 이를 통해 어떤 데이터 타입이든 일관된 포맷으로 클라이언트에 전달할 수 있게 되었습니다.
## 오늘 이슈
- Controller - Service - Repository로 이어지는 레이어드 아키텍처에서 데이터 전송 규격(DTO/파라미터)이 어긋나 발생한 컴파일 에러를 해결했습니다. 모든 라인의 '약속'을 일치시키는 과정에서 Spring Boot의 전체적인 흐름을 다시 한번 복습했습니다.
## 한 줄 요약
초급 프로젝트 들어간지 이틀째인데 남과 해보는 경험치 처음이라 아직까진 재밌다!
