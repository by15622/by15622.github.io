2026-04-17 학습 기록

## 오늘 한 것
- Service Test Mockito를 활용해 DB 없이도 비즈니스 로직(중복 검증, 권한 확인 등)이 정확히 작동하는지 검증하는 단위 테스트를 작성했습니다. 그리고 Controller Test MockMvc를 사용하여 실제 서버를 띄우지 않고도 API 엔드포인트가 올바른 HTTP 상태 코드(201, 200, 204)를 반환하는지 테스트했습니다. 
- @Slf4j를 사용하여 주요 비즈니스 로직의 성공/실패 시점에 로그를 남겼습니다. 특히 예외 발생 시 log.warn을 통해 추적 가능한 데이터를 남기도록 개선했습니다.
## 오늘 이슈
- 리뷰 등록/수정 시 응답으로 void를 반환했으나, 팀원으로부터 프론트엔드의 편의를 위해 생성/수정된 데이터를 직접 반환해달라는 피드백을 받았습니다. 해결 방안으로 API(Controller)뿐만 아니라 데이터를 직접 생성하는 주방장 역할인 서비스(Service) 레이어까지 모두 도움받아 리팩토링했습니다. 결과적으로 ReviewDto를 반환하게 하여 프론트엔드에서 추가적인 조회 요청 없이 화면을 즉시 갱신할 수 있도록 개선했습니다.
- 최신 버전 환경에서 기존 @MockBean 어노테이션이 인식되지 않는 빨간 줄 에러가 발생했습니다. 해결방안으로 스프링부트 3.4 버전부터 변경된 @MockitoBean으로 교체하여 최신 기술 스택에 맞는 테스트 환경을 구축했습니다.
## 한 줄 요약
어렵다.. 전쟁같은 하루였다 근데 코드가 더 잘보였으면 좋겠다 공부부족이 크게 느껴지는 하루다 