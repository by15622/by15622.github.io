2026-04-27 학습 기록

## 오늘 한 것
- 리뷰(Review)와 댓글(Comment) 도메인 간의 연동 테스트를 통해 1:N 관계의 비즈니스 로직을 검증했습니다. 특히 댓글의 생성,수정,삭제가 일어날 때, 상위 리뷰 엔티티의 commentCount가 실시간으로 증감하며 데이터 정합성이 유지되는 파이프라인을 최종 확인했습니다.
- 피그마 UI 시안을 분석하며 백엔드 API가 화면 요구사항을 완벽히 충족하는지 전체적으로 찾아봤습니다. 프론트엔드의 정렬 버튼 UI를 ReviewListRequest의 동적 파라미터와 매핑하여 유연한 데이터 반환 구조를 확보했습니다. 그리고 인기 리뷰 데이터가 없을 경우를 대비해 Map.of("content", ...) 형태의 래핑 구조를 적용하여, 프론트엔드에서 데이터가 비어있어도 렌더링 오류가 발생하지 않도록 안정적인 응답 구조를 설계했습니다.
## 오늘 이슈
- 논리 삭제(Soft Delete) 고도화를 위해 엔티티에서 @UniqueConstraint를 제거하고 애플리케이션 단에서 검증하도록 로직을 변경했으나, 배포 서버에서 여전히 uq_reviews_book_user 제약 조건 위반 에러(DataIntegrityViolationException)가 발생하는 현상이 나타났습니다. 원인으로 JPA의 hibernate.ddl-auto=update 옵션의 특성 때문이었습니다. 해당 옵션은 엔티티에 새로운 컬럼이 추가되는 등의 '확장'은 자동으로 반영해 주지만, 기존에 존재하던 제약 조건(Index, Unique Constraint 등)을 자바 코드에서 삭제했다고 해서 실제 DB 테이블에서 자동으로 DROP 해주지는 않는다는 사실을 확인했습니다. 해결방안으로 배포 서버 DB에 과거의 인덱스가 그대로 남아있음을 확인하고, 인프라 담당자에게 요청하여 ALTER TABLE reviews DROP INDEX uq_reviews_book_user; 쿼리를 직접 실행하여 수동으로 제약 조건을 제거했습니다. 이를 통해 로컬-서버 간 DB 스키마 동기화 문제를 성공적으로 해결했습니다.
## 한 줄 요약
오늘 테스트 커버리지랑 너무많은 작업을 건들여서 정신없는 하루였지만 다시 한번더 봐야겠다.