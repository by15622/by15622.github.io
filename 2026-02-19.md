2026-02-19 학습 기록

## 오늘 한 것
- 여러 요청이 동시에 파일에 접근할 때 발생하는 EOFException을 막기 위해 ReentrantLock을 관리하는 FileLockProvider를 생성하고, 각 Repository 레이어에서 파일 읽기/쓰기 시 lock()과 try-finally 블록을 사용하여 안전하게 자원을 해제하도록 로직을 도움받아 강화하였다.
- Google Java Style Guide 적용을 했으며, 협업을 위해 인텔리제이에 구글 코드 스타일 XML을 임포트하고, 저장 시 자동 정렬(Reformat code) 기능을 설정하였습니다.

## 오늘 이슈
- Interface와 Implementation 간의 메서드 불일치 (@Override 에러)가 일어났다. 상황으로 강사님이 제공한 인터페이스를 적용하자 클래스 곳곳에서 Method does not override... 에러 발생하였고 인터페이스에 없는 메소드가 발견되어서 메서드 이름과 매개변수(UUID id 등)를 인터페이스와 100% 동일하게 수정하였다. 

## 한 줄 요약
- 이제 자바기초 공부가 끝나가고 객체지향 들어가려는데 이게 무슨말인지 모르겠다..