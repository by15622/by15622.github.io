2026-04-03 학습 기록

## 오늘 한 것
- Spring Boot 코드가 Docker로 포장되어 ECR 창고에 저장되고, ECS(클러스터와 서비스)를 통해 실제 서버로 띄워지는 배포의 큰 그림을 파악했습니다.
- 프라이빗 저장소를 세팅하고, 소스 코드에 노출되면 안 되는 AWS Access Key와 DB 암호를 GitHub Secrets에 안전하게 도움받아 격리 보관했습니다.
## 오늘 이슈
- 윈도우 환경에서 작성된 코드가 리눅스 기반의 GitHub Actions 가상 환경으로 올라가면서, 빌드 파일(gradlew)의 실행 권한이 누락되었는데 .yml 워크플로우 파일의 빌드 단계 직전에 도움받아 run: chmod +x gradlew 명령어를 추가하여 실행 권한을 강제로 부여해 완벽 해결하였습니다.
## 한 줄 요약
독커부터 aws는 너무 어려워서 정신이없다