2026-03-27 학습 기록

## 오늘 한 것
- 기존에 만든 UserException을 부모 클래스로 두고, 이를 상속받는 자식 클래스 UserNotFoundException 등을 생성했다. 자식 클래스의 생성자 내부에 super(ErrorCode.USER_NOT_FOUND);를 작성하여, 객체가 생성될 때 부모 클래스의 생성자를 호출하고 에러 코드를 고정값으로 전달하도록 초기화 로직을 도움받아 짰다. 
- 기존 예외(IllegalArgumentException 등)와 커스텀 예외들을 처리하는 모든 @ExceptionHandler 메서드의 반환 타입을 ResponseEntity<ErrorResponse>로 통일했다. 그리고 에러 객체의 메서드(e.getClass().getSimpleName())를 호출하여 발생한 예외의 정확한 클래스 타입을 추출하고, 상태 코드(HttpStatus.BAD_REQUEST.value())를 정수형으로 추출하여 ErrorResponse 객체를 생성(초기화)한 뒤 응답 본문에 담아 반환하도록 도움받아 구현했다.
## 오늘 이슈
- 클래스 위에 @Slf4j를 선언했는데도 log.info() 등의 코드에서 log 변수에 빨간 줄(Cannot resolve symbol)이 발생했다. 코드가 틀린 것이 아니라, 인텔리제이 편집기 환경에서 롬복이 백그라운드에서 코드를 자동 생성해 주는 설정이 꺼져 있었다. 인텔리제이 Settings > Compiler > Annotation Processors 메뉴에서 Enable annotation processing 체크박스를 활성화하여 롬복과 IDE의 연결을 복구했다
## 한 줄 요약
오류관련은 할수록 너무 어렵다