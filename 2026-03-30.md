2026-03-30 학습 기록

## 오늘 한 것
- 비즈니스 로직의 핵심인 서비스 계층이 외부 환경(DB 등)에 의존하지 않고 독립적으로 정확하게 동작하는지 확인하는 작업을 진행했습니다. UserRepository, ChannelRepository 등 의존 관계에 있는 객체들을 가짜 객체(Mock)로 대체하여 테스트 속도를 높이고 로직 검증에 집중했습니다. 그리고 가짜 객체가 특정 상황에서 어떤 값을 돌려줄지(given) 미리 정해두고, 서비스 메서드가 이를 이용해 올바른 결과(UserDto 등)를 산출하는지 검증했습니다.
- 데이터베이스와 직접 소통하는 영속성 계층이 의도한 SQL 쿼리를 정확히 날리는지, JPA 설정이 올바른지 확인했습니다. 실제 운영 DB에 영향을 주지 않도록 테스트 실행 시에만 메모리에 떴다 사라지는 H2 데이터베이스 환경을 구축했습니다 그리고 유저나 메시지가 생성될 때 생성 시간(createdAt)이 자동으로 기록되는지 확인하고, 테스트 환경에서도 이 기능이 작동하도록 설정을 도움받아 주입했습니다.
## 오늘 이슈
- 컨트롤러 테스트 실행 시 서버(Context)가 뜨지 않고 에러 발생하였는데 원인이 컨트롤러가 의존하는 서비스 클래스(UserService, UserStatusService 등)가 테스트 환경에 등록되지 않아서 사용 중인 모든 서비스 객체를 @MockitoBean으로 선언하여 가짜 부품(Mock)으로 채워 넣어 해결하였다.
## 한 줄 요약
아프니까 공부가 힘들다 몸관리가 참 중요하다