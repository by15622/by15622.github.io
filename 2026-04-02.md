2026-04-02 학습 기록

## 오늘 한 것
- Spring Boot 코드 분석을 FileMetadataController 코드를 통해 REST API의 흐름 업로드, 조회, 삭제와 @RestController, @RequiredArgsConstructor 같은 주요 어노테이션의 역할을 학습했습니다.
- 터미널에서 SSH 키(pem) 파일의 경로를 설정하고 권한(chmod 400)을 변경하는 로컬 환경 세팅을 완료했습니다. 그리고 AWS EC2 서버에 성공적으로 접속하여 리눅스 환경을 확인하고, 비용 발생을 방지하기 위해 사용하지 않는 인스턴스를 '중지(Stop)'하는 운영 관리법을 익혔습니다. 
## 오늘 이슈
- SSH 키 파일 권한 및 경로 이슈가 있었는데 다운로드 폴더에 있는 키 파일이 터미널에서 보이지 않거나 권한 문제로 접속이 거절되었고 ~/.ssh 폴더를 생성해 키를 이동시키고, chmod 400 명령어로 보안 권한을 강화화였습니다.
- CLI 설치 후에도 aws: command not found 에러 발생하였습니다. 그래서 터미널을 완전히 종료 후 재실행하거나 시스템 재부팅을 통해 윈도우가 새로운 프로그램을 인식하도록 유도하였습니다.
## 한 줄 요약
AWS 정신없지만 신기하다