2026-04-17 학습 기록

## 오늘 한 것
- 리뷰 도메인 리팩토링 및 피드백 반영을 주로 했으며 팀 공통 규격에 맞춰 모든 API의 인증 헤더를 Deokhugam-Request-User-ID로 통일했습니다. 그리고 리뷰 등록 시 헤더 의존성을 제거하고, ReviewCreateRequest 바디(Body)에 userId를 포함하여 처리하도록 변경했습니다.
- 목록 조회 정렬 기능으로 orderBy(정렬 기준)와 direction(DESC/ASC) 파라미터를 추가하고, 이를 QueryDSL 동적 쿼리에 연동하여 최신순/평점순 정렬이 가능하게 했습니다. 그리고, 프론트엔드 연동 규격(Swagger)에 맞춰 닉네임 필드명을 nickname에서 userNickname으로 롤백하고, MapStruct 매핑을 완료했습니다.
## 오늘 이슈
- 리뷰어로부터 "파라미터로 직접 받으니 DTO(ReviewListRequest)가 필요 없다"는 삭제 의견을 받았습니다. 해결방안으로 파라미터가 8~9개인 상황에서 DTO를 지우면 'Long Parameter List'라는 코드 악취가 발생함을 인지. Parameter Object 패턴의 장점을 설명하며 DTO를 유지함으로써 코드의 가독성과 유지보수성을 지켜냈습니다.
## 한 줄 요약
오늘이 가장 힘든날이다 리뷰는.. 정말 너무 어렵다.