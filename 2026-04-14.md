2026-04-08 학습 기록

## 오늘 한 것
- 중급 프로젝트 기획서의 '1인 1리뷰', '논리 삭제', '반정규화 카운트 필드' 요구사항을 반영하여 Review 엔티티를 작성했다. @UniqueConstraint를 사용해 DB 레벨에서 (book_id, user_id) 복합 유니크 키를 설정하여 중복 리뷰 작성을 원천 차단하고, 성능 최적화를 위해 좋아요 수(like_count)와 댓글 수(comment_count)를 반정규화하여 기본값 0으로 설정했다.
- Controller & Service 계층 분리 및 리뷰 등록 API 관련하여 ReviewService(1인 1리뷰 검사)을 수행하고, DTO를 Entity로 변환하여 DB에 저장하는 실제 계산/처리 로직을 작성했다. (@Transactional 적용) ReviewController 인터넷(HTTP) 요청을 수신하고 상태 코드(201 Created)를 응답하는 입구 역할을 구현했다.
## 오늘 이슈
- 공통으로 사용하는 BaseEntity를 상속받아 Review 엔티티를 만들었는데, 계속해서 PK(기본키)가 없다는 JPA 에러가 발생했다. 부모 클래스에 분명히 @Id가 있었기에 원인을 찾기 어려웠다. 원인으로 팀원이 BaseEntity를 초기 세팅할 때, 자동 완성의 실수로 JPA용 @Id(jakarta.persistence.Id)가 아닌 Spring Data의 NoSQL용 @Id(org.springframework.data.annotation.Id)를 임포트해 둔 것이 원인이었다. 공통 파일의 버그임을 인지하고, 당장 내 작업이 막히지 않도록 임시로 상속을 끊고 Review 엔티티 안에 필드를 직접 넣어 테스트를 진행했다. 이후 팀원들에게 상황을 공유하고 올바른 jakarta.persistence.Id로 임포트를 수정하기로 했다.
## 한 줄 요약
같이하면 늘 어렵고 하나하나 맞춰나가야 하지만 같이하는 재미가 있어서 재밌다. 