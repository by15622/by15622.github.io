2026-03-19 학습 기록

## 오늘 한 것
- 프론트엔드 연동을 위해 수정된 팀원분의 feature/dashboard 브랜치 코드를 내 로컬 브랜치(feature/index-data)로 성공적으로 병합했다.
- 대량의 시계열 데이터를 다루는 과정에서 발생할 수 있는 결측치(Null)를 BigDecimal.ZERO로 보정하는 정제 로직과, 조회 성능 최적화를 위한 커서 기반 페이징(Cursor-based Pagination) 로직을 도움받아 적용했다.
- Swagger 어노테이션(@Operation, @Tag)을 활용한 API 명세서 분리, 전체 리소스를 교체하는 PUT 대신 변경된 데이터만 반영하는 PATCH 메서드(@PatchMapping)를 채택한 이유 등 RESTful API 설계 의도를 정리했다.
## 오늘 이슈
- 테스트를 위해 로컬 DB에서 DROP TABLE로 테이블을 날렸는데, 스프링 서버를 재시작해도 테이블이 다시 생성되지 않는 상황이 발생하였다. application.yml의 JPA 설정이 ddl-auto: none으로 되어 있었고, 설정을 잠시 update로 변경하여 서버를 가동, 엔티티(Entity)를 기반으로 테이블을 성공적으로 복구한 뒤 다시 none으로 원상 복구하여 해결했다.
## 한 줄 요약
충돌과 다양한 문제가 많았지만 그래도 띄워서 보람차다