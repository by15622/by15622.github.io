2026-02-24 학습 기록

## 오늘 한 것
- API RESTful 재설계 및 구현을 해봤습니다. 기존의 동사 중심 주소(예: /create, /update)를 자원 중심의 표준 REST API 주소 체계로 전면 수정했습니다.
- GET, POST, PATCH, DELETE 메서드를 용도에 맞게 적용을 했습니다. 수정된 API가 정상 작동하는지 포스트맨으로 직접 테스트하고, 테스트 결과를 JSON 파일로 Export 했습니다.

## 오늘 이슈
- 채널 수정 API 테스트 시 응답은 200 OK가 뜨는데 정작 이름은 바뀌지 않는 현상이 발생했습니다. name을 요구하는데 코드(DTO)에서는 newName으로 되어 있어 데이터가 제대로 매핑되지 않았습니다 그래서 DTO의 필드명을 스펙에 맞춰 수정하고, 이와 연결된 서비스 로직까지 동기화하여 해결했습니다.

## 한 줄 요약
- 객체지향 공부하자!!!!!