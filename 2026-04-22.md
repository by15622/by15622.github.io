2026-04-17 학습 기록

## 오늘 한 것
- 리뷰를 삭제한 사용자가 동일한 도서에 다시 리뷰를 남길 수 없던 문제를 해결했습니다. DB의 Unique 제약 조건을 제거하는 대신, 서비스 레이어에서 deletedAtIsNull 쿼리를 통해 현재 살아있는 리뷰만 체크하도록 로직을 고도화했습니다. 그리고 이미 삭제된 리뷰 ID를 직접 입력해 수정하거나, 좋아요를 누르거나, 상세 조회를 시도하는 모든 경로를 차단했습니다. 서비스 로직 최상단에 getDeletedAt() != null 체크를 일괄 적용하여 시스템의 보안성을 한 단계 높였습니다.
- BigDecimal과 RoundingMode.HALF_UP을 사용하여 소수점 둘째 자리까지 정확한 평균 평점을 계산하도록 구현했습니다. 그리고 리뷰 등록 시뿐만 아니라, 리뷰 삭제 시에도 기존 평점 합계에서 해당 점수를 차감하고 평균을 재계산하는 removeBookStats 로직을 추가하여 데이터 불일치 문제를 원천 봉쇄했습니다.
## 오늘 이슈
- 리뷰를 삭제(논리 삭제)한 유저가 같은 책에 다시 리뷰를 쓰려고 하면 DB 제약 조건 위반 에러 발생하였습니다. 원인으로 DB의 Unique Index는 deleted_at이 찍힌 데이터도 '존재'하는것으로 판단하였습니다. DB 레벨의 제약 조건을 제거하고, 애플리케이션 레벨(Service)에서 deleted_at IS NULL인 데이터만 체크하도록 도움받아 중복 검증 로직 변경하였습니다.
- 최신순/과거순 정렬 시 데이터가 중복되거나 누락되는 현상이 발생하였습니다. 원인으로 정렬 조건(Order)은 동적인데, 다음 페이지를 찾는 커서 조건(Cursor)이 한 방향으로 고정되어 있었습니다. 해결방안으로 정렬 방향에 따라 커서 연산자(gt, lt)가 대칭되도록 수정하고, 2차 정렬 기준(ID)을 도움받아 적용해 데이터 고유성 보장하였습니다.
## 한 줄 요약
어제에 비하면 오늘은 맘이 편하다..