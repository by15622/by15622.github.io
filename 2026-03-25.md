2026-03-25 학습 기록

## 오늘 한 것
- 하나의 application.yaml에 있던 설정을 개발 환경(dev)과 운영 환경(prod)으로 분리를 해봤다. 로컬에서 개발할 때는 테이블 자동 업데이트(ddl-auto: update)와 상세한 SQL 로그를 보고, 실제 운영 서버에서는 안전을 위해 테이블 자동 변경을 막고 로그를 최소화하도록 설정했다.
- 자바의 기본 예외 클래스들은 에러 메시지 하나만 텍스트로 달랑 남길 수 있어서 세밀한 에러 추적이 어렵다는 단점을 배웠다. 이를 해결하기 위해 발생 시간(timestamp), 에러 코드(ErrorCode), 상세 정보(details)를 객체 안에 통째로 담을 수 있는 최상위 커스텀 예외 클래스 DiscodeitException 뼈대를 만들었다. 이 과정에서 객체가 생성될 때 무조건 한 번 실행되어 초기값을 세팅하는 '생성자'의 개념과, 파라미터가 다르면 여러 개 만들 수 있는 '오버로딩'의 원리를 코드로 도움받아 직접 구현해 보았다.
## 오늘 이슈
- 오늘 주요 메서드에 로그를 추가하던 중, 예외가 터지는 상황에서 어떤 로그 레벨을 써야 할지 고민이 되었는데 강사님께 피드백을 구했고, "ERROR가 맞을지 WARN이 맞을지 생각해보라"는 조언을 통해 그 기준을 명확하게 잡을 수 있었다.
## 한 줄 요약
실습7 화이팅! 초급프로젝트가 끝나고 다시 시작하니까 들어오는게 조금 많아졌다.