2026-04-08 학습 기록

## 오늘 한 것
- 서비스 운영을 위한 ECS 클러스터를 생성함. 프리티어 유지를 위해 t2.micro 인스턴스를 사용하는 Self-managed 방식으로 구축을 완료하였다. 그리고, 도커 이미지를 어떻게 실행할지 결정하는 설계도를 작성해서 ECR에 올려둔 최신 이미지를 연결하고, CPU와 메모리 리소스를 프리티어 사양(0.25 vCPU, 0.5 GB)에 맞춰 최적화하였다.
- 보안을 위해 민감한 정보를 담은 .env 파일을 S3 버킷에 보관하고, 태스크 실행 시 자동으로 불러오도록 설정하였다. 그리고 ECS 서비스가 S3에 접근할 수 있도록 ecsTaskExecutionRole에 AmazonS3ReadOnlyAccess 정책을 추가하여 보안 통로를 확보하였다.
## 오늘 이슈
- AWS가 추천하는 Managed Instances 옵션으로 생성시, t2.micro 사양을 충족하지 못한다는 CreateCapacityProvider 에러 발생하였는데 원인은 최신 자동화 기능이 구형 프리티어 인스턴스 타입과 호환성 문제가 있었고 동기들의 집단지성을 활용해 Self-managed instances 옵션으로 변경하여 해결하였습니다.
## 한 줄 요약
ecs가 제일 어려운거 같은데 재밌다 