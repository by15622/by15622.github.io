2026-04-06 학습 기록

## 오늘 한 것
- 데이터베이스 비밀번호와 AWS 접속 키 같은 민감한 정보를 docker-compose.yml에 직접 적지 않고, .env 파일로 분리하여 관리하는 방법을 적용했다. env_file: .env 옵션과 environment 항목을 조합하여 스프링 부트 애플리케이션과 PostgreSQL 컨테이너가 정상적으로 연동되어 실행되는 것(Started DiscodeitApplication)을 확인했다.
- AWS S3 버킷 생성 및 IAM 권한 설정관련 파일 저장을 위한 AWS S3 버킷(discodeit-binary-content-storage-seohyunha)을 생성했다. 스프링 부트가 이 S3에 접근할 수 있도록 IAM 사용자(discodeit)를 만들고 AmazonS3FullAccess 권한을 부여한 뒤, 액세스 키(Access Key)와 시크릿 키(Secret Key)를 발급받아 .env 파일에 저장했다.
## 오늘 이슈
- 작업을 마치고 git push를 시도했으나 아래와 같은 에러가 발생하며 업로드가 강제 차단되었다. Push cannot contain secrets: .env 파일에 적힌 AWS 시크릿 키가 깃허브에 노출될 위험이 있어 깃허브가 자체적으로 푸시를 막고 대용량 DB 파일 추적: 로컬 데이터베이스 폴더인 postgres_data 내부의 수많은 실행 파일들이 Git의 추적 대상에 포함되었다.
## 한 줄 요약
한 번 고생하고 다시하니까 좀 재밌다